name: PRINT THAT FLAG
on:
  pull_request_target:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    environment: CTF
    steps:
      - name: Print FLAG Test fork
        #run: "echo $FLAG | base64"
        run: "echo FLAG test 2"
        env:
          FLAG: ${{ secrets.FLAG }}